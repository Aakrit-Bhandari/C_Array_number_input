#include<stdio.h>
#include<stdlib.h>
#include<math.h>
int main(void)
{
int a;
printf("Enter the total number of inputs in arr=");
scanf("%d",&a);
  int arr[a];
  for (int i=0; i<a; ++i)
    {
    scanf("%d",&arr[i]);
    }
    printf("_______OUtput_______")
    for (int i=0;i<a;++i)
    {
    printf("%d ",arr[i]);
    }
}
